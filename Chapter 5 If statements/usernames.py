#using if statements we will define a list of usernames and test if they have been taken
usernames = []
#checking if the username is taken

for user in usernames:
    if usernames == []:
        print('we need to find a new username')
        
