# this will define a function to display a message and the name of a user 
def display_message(username):
    """Display a simple message."""
    print(f"Hello {username}, today we are learning about functions, and how they are defined.") # printing a simple message
    
# calling the function
display_message('Travis') # calling the function8u