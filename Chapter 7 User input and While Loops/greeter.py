# Using user input to greet the user
prompt = "If you tell us who you are, we can personalize the messages you see."
prompt += "\nWhat is your first name? "
name = input(prompt)
print(f"Hello, {name}!")

age = input("How old are you? ")
age = int(age)
age >= 30

#print(f"{name}, you are {age} years old.")
# Using user input to greet the user